{
  "name": "hangman",
  "version": "1.0.0",
  "description": "**Edit a file, create a new file, and clone from Bitbucket in under 2 minutes**",
  "main": "pokemonDetails.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "css": "node-sass --include-path scss --watch scss/main.scss dist/main.css",
    "dev": "reload -b | node-sass --include-path scss --watch scss/style.scss dist/style.css | node-sass --include-path scss --watch scss/pokemonDetails.scss dist/pokemonDetails.css"
  },
  "author": "Alexa Duarte",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^4.13.1",
    "reload": "^3.0.4"
  }
}
