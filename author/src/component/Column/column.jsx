import React from 'react';

const Column = ( { name, author, books, bornDate } ) => {
    
    return(
        <Column>

        </div>
    );
};

Row.propTypes = {
    name: React.PropTypes.string.isRequired
}

export default Column;